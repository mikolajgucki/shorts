<!-- #npm #javascript #typescript -->

# Publish

```sh
# Set access token (must be granular access token)
npm config set //registry.npmjs.org/:_authToken=TOKEN_HERE

# Publish as a public package
npm publish --access public
```